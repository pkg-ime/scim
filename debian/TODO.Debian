1.2.0
----------------------
Some way to specify shlibs as >= 1.0.0 instead of -V (which will give >=
${Source-Version})

Tighten xlibs-dev build-dependence (perhaps to libx11-dev and x-dev, but
right now the configure script requires libxt-dev for X11/Intrinsic.h)
